import React from 'react';
import ReactDOM from 'react-dom';
import ScrollUpButton from '../../dist/react-scrollup';

const rootEl = document.getElementById('ReactRoot');
ReactDOM.render(
  <div>
    <ScrollUpButton />
  </div>,
  rootEl
);
